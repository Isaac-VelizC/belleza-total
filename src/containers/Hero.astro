---
import ImgHero from "../assets/images/hero.png";
import CtaButton from "@components/ui/CtaButton.astro";
---

<section
  id="hero"
  class="relative h-auto py-20 lg:py-0 lg:min-h-screen flex items-center bg-cover bg-center bg-no-repeat overflow-hidden"
  style={`background-image: url(${ImgHero.src})`}
>
  {/* Elementos decorativos animados */}
  <div class="absolute top-20 left-10 w-32 h-32 bg-secondary/20 rounded-full blur-xl animate-pulse"></div>
  <div class="absolute bottom-20 right-10 w-24 h-24 bg-accentcolor/20 rounded-full blur-lg animate-bounce"></div>
  <div class="absolute top-1/2 left-1/4 w-16 h-16 bg-primary/10 rounded-full blur-md animate-pulse delay-300"></div>

  {/* Contenido principal */}
  <div class="container mx-auto px-6 lg:px-16 relative z-10">
    <div class="w-full lg:w-3/5">
      <div class="md:text-center xl:text-left flex flex-col justify-center space-y-6">
         {/* Título */}
          <h1 class="text-5xl md:text-6xl lg:text-[82px] font-bold leading-tight text-primary">
            <span>Brilla con Confianza,</span>
            <span class="bg-gradient-to-r from-primary via-secondary to-accentcolor bg-clip-text text-transparent font-extrabold">
              Sé Tú Misma
            </span>
          </h1>

        {/* Subtítulo */}
        <p class="text-sm lg:text-lg text-texto/70 md:max-w-lg lg:max-w-2xl mx-auto lg:mx-0 leading-relaxed">
          Cortes, coloración, tratamientos y mucho más para realzar tu estilo.
          Reserva tu cita en línea fácilmente y disfruta de promociones
          exclusivas.
        </p>

        {/* Botones CTA */}
        <div class="flex flex-col mt-14 md:mt-4 lg:mt-0 sm:flex-row gap-4 justify-center lg:justify-start">
          <CtaButton
            href="#contact"
            text="Reserva tu cita ahora"
            variant={"primary"}
          >
            <svg
              slot="iconEnd"
              class="w-5 h-5 group-hover:translate-x-1 transition-transform duration-300"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </CtaButton>

          <CtaButton
            href="#servicios"
            text="Ver Servicios"
            variant={"secondary"}
          >
            <svg
              slot="iconEnd"
              class="w-5 h-5 group-hover:rotate-90 transition-transform duration-300"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"
              ></path>
            </svg>
          </CtaButton>
        </div>

        {/* Prueba social */}
        <div class="flex flex-wrap gap-6 justify-center lg:justify-start text-textmuted/80 mt-4 text-sm font-medium">
          <div class="flex items-center gap-2">
            <div class="w-3 h-3 bg-accentcolor rounded-full animate-pulse">
            </div>
            Profesionales Certificadas
          </div>
          <div class="flex items-center gap-2">
            <div class="w-3 h-3 bg-secondary rounded-full animate-pulse delay-150"></div>
            Productos Premium
          </div>
          <div class="flex items-center gap-2">
            <div class="w-3 h-3 bg-primary rounded-full animate-pulse delay-300"></div>
            10+ Años de Experiencia
          </div>
        </div>
      </div>
    </div>
  </div>

  {/* Elementos decorativos extra */}
  <div
    class="absolute -bottom-4 -left-4 w-20 h-20 bg-accentcolor/30 rounded-full blur-md animate-bounce delay-500"
  >
  </div>
  <div
    class="absolute top-4 right-8 w-16 h-16 bg-secondary/40 rounded-full blur-sm animate-pulse delay-700"
  >
  </div>

  {/* Indicador de scroll */}
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <svg
      class="w-6 h-6 text-primary"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>
</section>
